import { Component } from '@angular/core';

@Component({
    selector: 'app-game-stats',
    standalone: true,
    template: ` <div class="card">
        <div class="font-semibold text-xl mb-4">Empty Page</div>
        <p>Game Stats Works!</p>
    </div>`
})
export class GameStats {}
